# Imperial 1280 by 800 tactical planet

`actual.png` shows TACTICAL 5502 with palette 5532 through the uniform
letterboxed canvas. `result.json` records the same source-bound resource and
runtime contract.
